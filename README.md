Created a Python Script to test a website using Selenium framework in VS Code 
So, the script, initially tries to access the browser and then get the title name from that targeted website to print as a output.
